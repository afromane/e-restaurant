{% extends 'admin.html.twig' %}

{% block title %}Menu{% endblock %}


{% block body %}
    <div class="container-fluid p-0">

    <h1 class="h3 mb-3">Menu</h1>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                 {% for message in app.flashes('success') %}
                        <div class="alert alert-success">
                            {{ message }}
                        </div>
                    {% endfor %}

                        <a href="{{path('app_menu_new')}}" class="btn btn-primary"><i class="align-middle" data-feather="plus"></i> <span>Nouveau Menu</span><a>
                </div>
                <div class="card-body">
                     <table id="example1" class="table table-hover my-0">
                        <thead>
                            <tr>
                                <th class="">Menu</th>
                                <th class="">Action(s)</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for i in items %}
                            <tr>
                               
                                <td class="">{{ i.label }}</td>
                                <td>
                                    {# <a href="{{ path('app_menu_update', {id: '0915_'~i.id ~i.label ~'#'~i.id }) }}" class="btn btn-primary"><i class="align-middle" data-feather="edit"></i><a> #}
                                </td>
                            </tr>
                        {% endfor %}
                    </table>
                </div>
            </div>
        </div>
    </div>

    

       
    </div>


{% endblock %}


