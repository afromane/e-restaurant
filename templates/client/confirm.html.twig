{% extends 'admin.html.twig' %}

{% block title %}Client{% endblock %}


{% block body %}
    <div class="container-fluid p-0">

    <h1 class="h3 mb-3">Client Confirmer</h1>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                 {% for message in app.flashes('success') %}
                        <div class="alert alert-success">
                            {{ message }}
                        </div>
                    {% endfor %}

                        <a  target="_blank" href="{{path('app_client_confirmer_pdf')}}" class="btn btn-primary"><i class="align-middle" data-feather="printer"></i> <span>Pdf</span><a>
                </div>
                <div class="card-body">
                     <table id="example1" class="table table-hover my-0">
                        <thead>
                            <tr>
                                <th class="">Nom Client</th>
                                <th class="">Contact </th>
                                <th class="">Email</th>
                                <th class="">Date reservation</th>
                                <th class="">Place</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for i in items %}
                            <tr>
                                <td class="">{{ i.nomPrenom | upper }}</td>
                                <td class="">{{ i.phone }}</td>
                                <td class="">{{ i.email }}</td>
                                <td class="">{{ i.dateReserv | date('d-m-Y') }}</td>
                                <td class="">{{ i.place}}</td>
                                
                            </tr>
                        {% endfor %}
                    </table>
                </div>
            </div>
        </div>
    </div>

    

       
    </div>


{% endblock %}


